# The library is dependency-free. These are development-only dependencies (not required to use the library).

pytest      ~= 6.0
coverage    ~= 5.2
mypy        == 0.782
pycodestyle >= 2.6

# Documentation.
sphinx           == 3.2.1
sphinx_rtd_theme == 0.5.0
sphinx-computron <  2.0

# Packaging and distribution.
setuptools >= 46.0
wheel      >= 0.34, < 2.0
twine      >= 3.2
